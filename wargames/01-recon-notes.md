# Recon notes — 01-ad-economy-close-the-loop

- **R-A:** CodeFree service is session-scoped via `CodeFree.layer` in `packages/opencode/src/session/codefree.ts`. HTTP handlers use the Move 6 fork: `Wallet` + `AdStore` + core `completeImpression`/`creditAdClick` + exported `resolveUserID` (`handlers/codefree.ts`).
- **R-B:** Events emitted via `EventV2Bridge.publish` with `EventV2.define` schemas inline in `session/codefree.ts` (~lines 28–54); handlers duplicate `CreditUpdatedEvent` and call `events.publish`.
- **R-C:** Migrations registered in `packages/core/src/database/migration.gen.ts`; create with `cd packages/core && bun run migration --name <name>`.
- **R-D:** `schema.json` is generated by the same migration script when config schema changes.
- **R-E:** Transient UI mounts near `<Toast />` in `packages/tui/src/routes/session/index.tsx`; `AdBannerLive` added adjacent to toast overlay.
